/*#ifndef CUBE_H
#define	CUBE_H

class cytosol
{
public:
    //all of these are 1 larger than the actual, if 0 than there is nothing there
    unsigned int proteinIndex;
    unsigned int DNAIndex;
    unsigned int RNAIndex;
    cytosol(unsigned int pi, unsigned int di, unsigned int ri);
};

#endif*/	/* CUBE_H */

